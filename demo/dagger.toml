[modules.demo]
source = "."

[modules.demo.settings]
rustVersion = "1.98-trixie"
envFile = ".env"

[env.prod]
modules.demo.settings.envFile = ".env.prod"

# Clerk publishable key is necessary for build, but it doesn't have to be a real value
[env.ci]
modules.demo.settings.envFile = ".env.dist"

[modules.worker]
source = "github.com/sagikazarmark/daggerverse-beta/worker"

[modules.worker.settings]
bundleAsContainer = "demo:worker-bundle-as-container"
config = "wrangler.toml"

[modules.dagger-gha]
source = "github.com/sagikazarmark/daggerverse-beta/dagger-gha"

[modules.dagger-gha.settings]
env = "ci"
